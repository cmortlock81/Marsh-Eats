const metrics = [{ label: "Commission", value: "8%" }, { label: "RNLI allocation", value: "1%" }, { label: "Launch region", value: "Kent" }];
export default function AdminDashboard() { return <main><p className="eyebrow">Platform controls</p><h1>Admin dashboard</h1><section>{metrics.map((metric) => <article key={metric.label}><span>{metric.label}</span><strong>{metric.value}</strong></article>)}</section><div className="panel"><h2>Operational tools</h2><p>Restaurant onboarding, order search, refund support, commission reporting, RNLI reporting, and audit-log review are backed by the API and PostgreSQL schema.</p></div></main>; }
